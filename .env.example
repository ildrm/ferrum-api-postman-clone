# Optional absolute path for the development database, streamed bodies, and logs.
# FERRUM_DATA_DIR=/absolute/path/to/ferrum-data

# Optional Rust tracing filter (for example: ferrum=debug,sqlx=warn).
# RUST_LOG=info
